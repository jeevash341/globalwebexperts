# AI Assistant (server-side only — never exposed to the browser)
# On Lovable this is provisioned automatically.
LOVABLE_API_KEY=

# On Vercel / self-hosted: Google Gemini API key (used when LOVABLE_API_KEY is absent)
GEMINI_API_KEY=
